# Build artifacts inside chromium/ — generated by build:dev / build:prod
chromium/manifest.json
chromium/config.js

# Build output
dist/

# Extension signing keys (NEVER commit)
keys/
*.pem

# Dependencies
node_modules/

# OS
.DS_Store
